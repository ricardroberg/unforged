import styled from "styled-components";

export function Main() {
  return (
    <MainContainer>
      <h1>Main</h1>
    </MainContainer>
  );
}

const MainContainer = styled.main`
  color: var(--white);
`;
